{
  "name": "tendrilflow",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first collaboration workspace for coding agents.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
